# Primary API Key (required)
DEEPSEEK_API_KEY=sk-your-key-here

# E2B Sandbox API Key (required for code execution)
E2B_API_KEY=e2b_your-key-here

# Server Configuration
PORT=3001
NODE_ENV=development
MAX_ITERATIONS=15

# ===== AUTH PROFILE ROTATION (optional) =====
# Add multiple API keys for automatic failover on rate limits
# The system will try keys in order and failover on 429/401/403 errors

# Additional DeepSeek keys (failover)
# DEEPSEEK_API_KEY_2=sk-your-backup-key
# DEEPSEEK_API_KEY_3=sk-your-third-key

# OpenAI fallback (used if all DeepSeek keys fail)
# OPENAI_API_KEY=sk-your-openai-key
# OPENAI_API_KEY_2=sk-your-openai-backup

# OpenRouter fallback (supports many models)
# OPENROUTER_API_KEY=sk-or-your-key

# Groq fallback (fast inference)
# GROQ_API_KEY=gsk_your-groq-key
